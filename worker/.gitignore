# Build artefacts — both are regenerated by `npm run worker:build`.
dist/
.generated/

# miniflare's local D1 / cache state for `wrangler dev --local`.
.wrangler/

# A local .dev.vars would carry a SETUP_TOKEN or similar — never commit one.
.dev.vars
